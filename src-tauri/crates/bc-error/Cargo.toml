[package]
name = "bc-error"
version = "0.1.0"
edition = "2021"
description = "Structured error types for Better Cloudflare"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
